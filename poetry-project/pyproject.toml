[project]
name = "poetry-project"
version = "0.1.0"
description = ""
authors = [
    {name = "ganpat8903",email = "ganpat.kumawat.reversebits@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4 (<4.10)",
    "requests (>=2.32.3,<3.0.0)"
]

[tool.poetry]
packages = [{include = "poetry_project", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
